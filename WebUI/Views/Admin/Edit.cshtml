@model Domain.Entities.Wear

@{
    ViewBag.Title = "Адмін панель: редактування товарів";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="card">
    <div class="card-header">
        <h3>Редагування товару "@Model.Name"</h3>
    </div>



    @using (Html.BeginForm("Edit", "Admin",
    FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        <div class="card-body">
            @Html.HiddenFor(m => m.WearId)
            @foreach (var property in ViewData.ModelMetadata.Properties)
            {

                    switch (property.PropertyName)
                    {
                        case "WearId":
                        case "ImageData":
                        case "ImageMimeType":
                            
                            break;
                        default:
                            <div class="form-group">
                                <label>@(property.DisplayName ?? property.PropertyName)</label>
                                @if (property.PropertyName == "Description")
                                {
                                    @Html.TextArea(property.PropertyName, null, new { @class = "form-control", rows = 5 })
                                }
                                else
                                {
                                    @Html.TextBox(property.PropertyName, null, new { @class = "form-control" })
                                }
                                @Html.ValidationMessage(property.PropertyName)
                            </div>
                            break;
                    }
                
            }
        </div>
        <div class="form-group">
            <div style="position:relative;">
                <label>Зображення</label>
                <a class='btn' href='javascript:;'>
                    Виберіть файл...
                    <input type="file" name="ImageFile" size="40"
                           style="position:absolute;z-index:2;top:0;
                                left:0;filter: alpha(opacity=0); opacity:0;
                    background-color:transparent;color:transparent;"
                           onchange='$("#upload-file-info").html($(this).val());'>
                </a>
                <span class='label label-info' id="upload-file-info"></span>
            </div>
            @if (Model.ImageData == null)
            {
                <div class="form-control-static">Картинка відсутня</div>
            }
            else
            {
                <img class="img-thumbnail" width="150" height="150"
                     src="@Url.Action("GetImage", "Wear",
                        new { Model.WearId })" />
            }
        </div>
   
        <div class="card-footer">
            <input type="submit" value="Зберегти" class="btn btn-primary" />
            @Html.ActionLink("Відміна", "Index", null, new { @class = "btn btn-secondary" })
        </div>
    }
</div>

